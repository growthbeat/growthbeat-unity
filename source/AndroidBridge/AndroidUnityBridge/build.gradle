apply plugin: 'java'
jar.baseName = 'growthbeat-unity-bridge'
version = '1.0.0'

sourceCompatibility = '1.7'
targetCompatibility = '1.7'

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
}